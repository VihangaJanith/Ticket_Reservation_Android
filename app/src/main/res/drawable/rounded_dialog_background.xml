<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="15dp" />
    <solid android:color="@android:color/white" /> <!-- Background color (you can change this) -->
<stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Border color (you can change this) -->
</shape>
